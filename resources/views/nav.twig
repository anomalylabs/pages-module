{% import options.get('macro', 'anomaly.module.pages::macro') as macro %}

<ul class="{{ options.list_class }}">
    {{ macro.links(options.parent ? pages.children(options.parent) : pages.top(), pages, template.page, options, 1) }}
</ul>
