{% import options.macro ?: "anomaly.module.pages::macro" as macro %}

<ul class="{{ options.list_class }}">
    {{ macro.links(options.parent ? options.parent.children : pages.topLevel(), pages, template.page, options) }}
</ul>
